# Negative syntax fixture retained for parser experiments.
# `0..N` is legacy syntax and is intentionally not part of the Stage-A
# roundtrip. Current Flow uses `for i in 0 to N`.

function main() -> i32 {
    let mut s: i32 = 0
    for i in 0..10 {
        s = s + i
    }
    return s
}
