[package]
name = "dns_demo"
version = "0.1.0"
description = "Demo for registry package dns (getaddrinfo)"
author = "Flow Contributors"
license = "MIT"
entry = "src/main.flow"

[dependencies]
dns = "0.1.0"

[dev-dependencies]
