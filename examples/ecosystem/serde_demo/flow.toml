[package]
name = "serde_demo"
version = "0.1.0"
description = "Demo for registry package serde (binary pack/unpack)"
author = "Flow Contributors"
license = "MIT"
entry = "src/main.flow"

[dependencies]
serde = "0.1.0"

[dev-dependencies]
