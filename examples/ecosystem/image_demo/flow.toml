[package]
name = "image_demo"
version = "0.1.0"
description = "Demo for registry package image (PNG + PPM decode)"
author = "Flow Contributors"
license = "MIT"
entry = "src/main.flow"

[dependencies]
image = "0.1.0"

[dev-dependencies]
