[package]
name = "collectionsx_demo"
version = "0.1.0"
description = "Demo for registry package collectionsx"
author = "Flow Contributors"
license = "MIT"
entry = "src/main.flow"

[dependencies]
collectionsx = "0.1.0"

[dev-dependencies]
