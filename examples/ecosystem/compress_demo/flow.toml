[package]
name = "compress_demo"
version = "0.1.0"
description = "Demo for registry package compress (zlib deflate/inflate)"
author = "Flow Contributors"
license = "MIT"
entry = "src/main.flow"

[dependencies]
compress = "0.1.0"

[dev-dependencies]
