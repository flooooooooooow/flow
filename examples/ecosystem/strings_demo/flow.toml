[package]
name = "strings_demo"
version = "0.1.0"
description = "Demo for registry package strings"
author = "Flow Contributors"
license = "MIT"
entry = "src/main.flow"

[dependencies]
strings = "0.1.0"

[dev-dependencies]
