[package]
name = "ffi_demo"
version = "0.1.0"
description = "Demo for registry package ffi (pack/unpack + sizeof ptr)"
author = "Flow Contributors"
license = "MIT"
entry = "src/main.flow"

[dependencies]
ffi = "0.1.0"

[dev-dependencies]
