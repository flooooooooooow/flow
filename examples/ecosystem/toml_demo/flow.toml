[package]
name = "toml_demo"
version = "0.1.0"
description = "Demo for registry package toml"
author = "Flow Contributors"
license = "MIT"
entry = "src/main.flow"

[dependencies]
toml = "0.1.0"

[dev-dependencies]
