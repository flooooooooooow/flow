import "stdlib/tensor.flow"

# vgpu compatibility case: Depth estimation execution

@gpu
function estimator(input: ptr<f32>, output: ptr<f32>) {
    # Compatibility placeholder for the depth estimator
}

function main() {
    print("depth stub")
}
