# @module examples.verify.math.derived
# @means  Septuple swap returns the original pair.
# @from   church — https://en.wikipedia.org/wiki/Product_type
# @tier   derived

# @means  seven nested swaps on a pair returns the pair.
# @from   church
# @tier   derived
# @needs  «Pair» «swap» «sextuple swap returns pair», «Pair» «swap» «quintuple swap returns pair»

theorem «Pair» «swap» «septuple swap returns pair» (a: Nat, b: Nat) {
    assume «Pair» «swap» «sextuple swap returns pair»(a, b)
    assume «Pair» «swap» «quintuple swap returns pair»(a, b)
    therefore swap(swap(swap(swap(swap(swap(swap(pair(a, b)))))))) == pair(a, b)
}

export «Pair» «swap» «septuple swap returns pair»