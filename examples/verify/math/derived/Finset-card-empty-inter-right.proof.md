# examples.verify.math.derived

*Right-empty intersection has cardinality zero.*

**Source.** graham-knuth-patashnik — *Concrete Mathematics*

## Derived fact 1 — card(s ∩ empty) = 0

**Coordinate.** Finset · cardinality · right empty intersection has size zero derived · **Derived fact**

*Source: graham-knuth-patashnik*

*Built on: intersection with empty has size zero, for intersection on Finset*

> **Goal.** card(s ∩ empty) = 0
>
> $$\forall s \in Finset\quad card(s ∩ empty) = 0$$

| | **Proof** | | **Math** |
|:---:|:---|:---:|:---|
| ① | We prove that right empty intersection has size zero derived for cardinality on Finset. |  |  |
| ② | We invoke the derived fact governing intersection on Finset: intersection with empty has size zero, for intersection on Finset (instantiated for s). |  |  |
| ③ | From step 2, this implies card(s ∩ empty) equals 0. Hence proven. | ③ | $card(s ∩ empty) = 0$ |

**Trace.** Each step lists the earlier steps it depends on.

| Step | Uses |
|:---:|:---|
| ③ | step 2 |

`Finset · cardinality · right empty intersection has size zero derived`
