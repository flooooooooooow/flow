# @module examples.verify.math.derived
# @means  Quintuple swap returns the original pair.
# @from   church — https://en.wikipedia.org/wiki/Product_type
# @tier   derived

# @means  swap(swap(swap(swap(swap(pair(a, b)))))) = pair(a, b).
# @from   church
# @tier   derived
# @needs  «Pair» «swap» «quadruple swap returns pair», «Pair» «swap» «triple swap returns pair»

theorem «Pair» «swap» «quintuple swap returns pair» (a: Nat, b: Nat) {
    assume «Pair» «swap» «quadruple swap returns pair»(a, b)
    assume «Pair» «swap» «triple swap returns pair»(a, b)
    therefore swap(swap(swap(swap(swap(pair(a, b)))))) == pair(a, b)
}

export «Pair» «swap» «quintuple swap returns pair»