# examples.verify.math.derived

*Left multiplying by an inverse yields the identity.*

**Source.** dummit-foote — *Abstract Algebra*, §1.1

## Derived fact 1 — inv(g) * g = 1

**Coordinate.** Group · inverse · left inverse multiplied on the left yields one · **Derived fact**

*Source: dummit-foote*

*Built on: left inverse recovers the identity, for inverse on Group*

> **Goal.** inv(g) * g = 1
>
> $$\forall g \in Group\quad inv(g) * g = 1$$

| | **Proof** | | **Math** |
|:---:|:---|:---:|:---|
| ① | We prove that left inverse multiplied on the left yields one for inverse on Group. |  |  |
| ② | We invoke the definitional clause governing inverse on Group: left inverse recovers the identity, for inverse on Group (instantiated for g). |  |  |
| ③ | From step 2, this implies inv(g) times g equals 1. Hence proven. | ③ | $inv(g) * g = 1$ |

**Trace.** Each step lists the earlier steps it depends on.

| Step | Uses |
|:---:|:---|
| ③ | step 2 |

`Group · inverse · left inverse multiplied on the left yields one`
