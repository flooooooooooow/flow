# examples.verify.math.derived

*A ring homomorphism sends zero plus zero to zero.*

**Source.** dummit-foote — *Abstract Algebra*, §7.3

## Derived fact 1 — f(0 + 0) = 0

**Coordinate.** RingHom · preservation · zero plus zero maps to zero · **Derived fact**

*Source: dummit-foote*

*Built on: homomorphisms preserve zero, for preservation on RingHom, zero plus zero is zero, for addition on Ring*

> **Goal.** f(0 + 0) = 0
>
> $$\forall f \in RingHom\quad f(0 + 0) = 0$$

| | **Proof** | | **Math** |
|:---:|:---|:---:|:---|
| ① | We prove that zero plus zero maps to zero for preservation on RingHom. |  |  |
| ② | We invoke the derived fact governing preservation on RingHom: homomorphisms preserve zero, for preservation on RingHom (instantiated for f). |  |  |
| ③ | We invoke the derived fact governing addition on Ring: zero plus zero is zero, for addition on Ring. |  |  |
| ④ | From step 2 and step 3, this implies f(0 plus 0) equals 0. Hence proven. | ④ | $f(0 + 0) = 0$ |

**Trace.** Each step lists the earlier steps it depends on.

| Step | Uses |
|:---:|:---|
| ④ | step 2 and step 3 |

`RingHom · preservation · zero plus zero maps to zero`
