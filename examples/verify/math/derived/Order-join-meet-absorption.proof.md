# examples.verify.math.derived

*Join absorbs meet as a derived lattice law.*

**Source.** davey-priestley — *Introduction to Lattices and Order*

## Derived fact 1 — join(a, meet(a, b)) = a

**Coordinate.** Order · join · join absorbs meet derived lattice · **Derived fact**

*Source: davey-priestley*

*Built on: join absorbs meet, for join on Order*

> **Goal.** join(a, meet(a, b)) = a
>
> $$\forall a \in \mathbb{N} \forall b \in \mathbb{N}\quad join(a, meet(a, b)) = a$$

| | **Proof** | | **Math** |
|:---:|:---|:---:|:---|
| ① | We prove that join absorbs meet derived lattice for join on Order. |  |  |
| ② | We invoke the derived fact governing join on Order: join absorbs meet, for join on Order (instantiated for a, b). |  |  |
| ③ | From step 2, this implies join(a, meet(a, b)) equals a. Hence proven. | ③ | $join(a, meet(a, b)) = a$ |

**Trace.** Each step lists the earlier steps it depends on.

| Step | Uses |
|:---:|:---|
| ③ | step 2 |

`Order · join · join absorbs meet derived lattice`
