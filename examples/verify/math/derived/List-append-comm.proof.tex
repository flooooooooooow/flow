\documentclass[11pt]{article}
\usepackage[T1]{fontenc}
\usepackage{textcomp}
\usepackage[margin=0.75in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{array,booktabs}
\usepackage{hyperref}
\setlength{\parindent}{0pt}
\newtheorem{theorem}{Theorem}
\theoremstyle{definition}
\newtheorem{definition}{Definition}
\title{Flow Proof Artifact: List-append-comm}
\author{Generated by \texttt{flow doc proof}}
\date{Flow Proof Book}
\begin{document}
\maketitle
List append commutes when lengths match trivially.\\[0.5em]
\textbf{Source.} church — https://en.wikipedia.org/wiki/Associative\_property\\[0.5em]
\bigskip
\noindent{\large \textbf{Derived fact 1.} \textit{For singleton left, cons(x, nil) ++ ys = ys with head x prepended via singleton law} \hfill List \cdot append \cdot singleton blocks commute with append}\par
\smallskip\noindent\textit{Coordinate: List \cdot append \cdot singleton blocks commute with append}\par
\smallskip\noindent\textit{Source: church}\par
\smallskip\noindent\textit{Built on: singleton prepends the head, for append on List, parentheses do not matter, for append on List}\par
\medskip
\noindent\textbf{Goal.} For singleton left, cons(x, nil) ++ ys = ys with head x prepended via singleton law\par
\noindent$\displaystyle \forall x \in \mathbb{N} \forall xs \in List \forall ys \in List\quad cons(x, nil) ++ (xs ++ ys) = (cons(x, nil) ++ xs) ++ ys$\par
\medskip
\noindent
\begin{tabular}{@{} >{\raggedright\arraybackslash}p{0.06\textwidth} >{\raggedright\arraybackslash}p{0.47\textwidth} >{\raggedleft\arraybackslash}p{0.41\textwidth} @{}}
\textbf{\#} & \textbf{Proof} & \textbf{Mathematics} \\
\midrule
\textbf{1.} & We prove that singleton blocks commute with append for append on List. & \\[0.45em]
\textbf{2.} & We invoke the definitional clause governing append on List: singleton prepends the head, for append on List (instantiated for x, xs). & \\[0.45em]
\textbf{3.} & We invoke the derived fact governing append on List: parentheses do not matter, for append on List (instantiated for cons(x, nil), xs, ys). & \\[0.45em]
\textbf{4.} & From step 2 and step 3, this implies cons(x, nil) plus plus (xs plus plus ys) equals (cons(x, nil) plus plus xs) plus plus ys. Hence proven. & $\displaystyle cons(x, nil) ++ (xs ++ ys) = (cons(x, nil) ++ xs) ++ ys$ \\[0.45em]
\bottomrule
\end{tabular}
\label{thm:List-append-singleton_blocks_commute_with_append}
\par\medskip\hrule
\end{document}
