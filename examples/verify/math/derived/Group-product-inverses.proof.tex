\documentclass[11pt]{article}
\usepackage[T1]{fontenc}
\usepackage{textcomp}
\usepackage[margin=0.75in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{array,booktabs}
\usepackage{hyperref}
\setlength{\parindent}{0pt}
\newtheorem{theorem}{Theorem}
\theoremstyle{definition}
\newtheorem{definition}{Definition}
\title{Flow Proof Artifact: Group-product-inverses}
\author{Generated by \texttt{flow doc proof}}
\date{Flow Proof Book}
\begin{document}
\maketitle
The inverse of a product reverses the factors.\\[0.5em]
\textbf{Source.} dummit-foote — *Abstract Algebra*, §1.1\\[0.5em]
\bigskip
\noindent{\large \textbf{Derived fact 1.} \textit{inv(a * b) = inv(b) * inv(a)} \hfill Group \cdot inverse \cdot product inverses reverse order}\par
\smallskip\noindent\textit{Coordinate: Group \cdot inverse \cdot product inverses reverse order}\par
\smallskip\noindent\textit{Source: dummit-foote}\par
\smallskip\noindent\textit{Built on: right inverse recovers the identity, for inverse on Group, inverses are unique, for inverse on Group, parentheses do not matter, for multiplication on Group}\par
\medskip
\noindent\textbf{Goal.} inv(a * b) = inv(b) * inv(a)\par
\noindent$\displaystyle \forall a \in Group \forall b \in Group\quad inv(a \cdot b) = inv(b) * inv(a)$\par
\medskip
\noindent
\begin{tabular}{@{} >{\raggedright\arraybackslash}p{0.06\textwidth} >{\raggedright\arraybackslash}p{0.47\textwidth} >{\raggedleft\arraybackslash}p{0.41\textwidth} @{}}
\textbf{\#} & \textbf{Proof} & \textbf{Mathematics} \\
\midrule
\textbf{1.} & We prove that product inverses reverse order for inverse on Group. & \\[0.45em]
\textbf{2.} & We invoke the derived fact governing inverse on Group: right inverse recovers the identity, for inverse on Group (instantiated for a). & \\[0.45em]
\textbf{3.} & We invoke the derived fact governing inverse on Group: right inverse recovers the identity, for inverse on Group (instantiated for b). & \\[0.45em]
\textbf{4.} & We invoke the derived fact governing inverse on Group: inverses are unique, for inverse on Group (instantiated for a * b, inv(b) * inv(a), inv(a * b)). & \\[0.45em]
\textbf{5.} & We invoke the definitional clause governing multiplication on Group: parentheses do not matter, for multiplication on Group (instantiated for a, b, inv(b)). & \\[0.45em]
\textbf{6.} & From step 2, step 3, step 4, and step 5, this implies inv(a times b) equals inv(b) times inv(a). Hence proven. & $\displaystyle inv(a \cdot b) = inv(b) * inv(a)$ \\[0.45em]
\bottomrule
\end{tabular}
\label{thm:Group-inverse-product_inverses_reverse_order}
\par\medskip\hrule
\end{document}
