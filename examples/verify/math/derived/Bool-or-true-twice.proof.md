# examples.verify.math.derived

*True disjoined with itself is true.*

**Source.** boole — https://en.wikipedia.org/wiki/Boolean_algebra

## Derived fact 1 — true or true = true

**Coordinate.** boolean truth values · disjunction · true disjoined with itself is true · **Derived fact**

*Source: boole*

*Built on: true is absorbing on the left, for disjunction on boolean truth values*

> **Goal.** true or true = true
>
> $$\mathsf{true} \lor \mathsf{true} = \mathsf{true}$$

| | **Proof** | | **Math** |
|:---:|:---|:---:|:---|
| ① | We prove that true disjoined with itself is true for disjunction on boolean truth values. |  |  |
| ② | We invoke the derived fact governing disjunction on boolean truth values: true is absorbing on the left, for disjunction on boolean truth values (instantiated for true). |  |  |
| ③ | From step 2, this implies the disjunction of true and true equals true. Hence proven. | ③ | $\mathsf{true} \lor \mathsf{true} = \mathsf{true}$ |

**Trace.** Each step lists the earlier steps it depends on.

| Step | Uses |
|:---:|:---|
| ③ | step 2 |

`boolean truth values · disjunction · true disjoined with itself is true`
