# examples.verify.math.derived

*Zero on the right is an additive identity for integers.*

**Source.** landau — *Foundations of Analysis*, Ch. 1

## Derived fact 1 — m + 0 = m

**Coordinate.** the integers · addition · zero on the right gives the value · **Derived fact**

*Source: landau*

*Built on: zero is the left identity, for addition on the integers, order does not matter, for addition on the integers*

> **Goal.** m + 0 = m
>
> $$\forall m \in \mathbb{Z}\quad m + 0 = m$$

| | **Proof** | | **Math** |
|:---:|:---|:---:|:---|
| ① | We prove that zero on the right gives the value for addition on the integers. |  |  |
| ② | We invoke the definitional clause governing addition on the integers: zero is the left identity, for addition on the integers (instantiated for m). |  |  |
| ③ | We invoke the derived fact governing addition on the integers: order does not matter, for addition on the integers (instantiated for m, 0). |  |  |
| ④ | From step 2 and step 3, this implies m plus 0 equals m. Hence proven. | ④ | $m + 0 = m$ |

**Trace.** Each step lists the earlier steps it depends on.

| Step | Uses |
|:---:|:---|
| ④ | step 2 and step 3 |

`the integers · addition · zero on the right gives the value`
