# examples.verify.math.derived

*Zero on the right annihilates real multiplication.*

**Source.** landau — *Foundations of Analysis*

## Derived fact 1 — x * 0 = 0

**Coordinate.** the real numbers · multiplication · zero on the right gives zero · **Derived fact**

*Source: landau*

*Built on: zero on the left gives zero, for multiplication on the real numbers, order does not matter, for multiplication on the real numbers*

> **Goal.** x * 0 = 0
>
> $$\forall x \in \mathbb{R}\quad x \cdot 0 = 0$$

| | **Proof** | | **Math** |
|:---:|:---|:---:|:---|
| ① | We prove that zero on the right gives zero for multiplication on the real numbers. |  |  |
| ② | We invoke the derived fact governing multiplication on the real numbers: zero on the left gives zero, for multiplication on the real numbers (instantiated for x). |  |  |
| ③ | We invoke the derived fact governing multiplication on the real numbers: order does not matter, for multiplication on the real numbers (instantiated for x, 0). |  |  |
| ④ | From step 2 and step 3, this implies x times 0 equals 0. Hence proven. | ④ | $x \cdot 0 = 0$ |

**Trace.** Each step lists the earlier steps it depends on.

| Step | Uses |
|:---:|:---|
| ④ | step 2 and step 3 |

`the real numbers · multiplication · zero on the right gives zero`
