# @module examples.verify.math.derived
# @means  Real multiplication associates.
# @from   landau — *Foundations of Analysis*
# @tier   derived

# @means  (x * y) * z = x * (y * z).
# @from   landau
# @tier   derived
# @needs  «Real» «multiplication» «one is the left identity», «Real» «multiplication» «one is the right identity», «Real» «multiplication» «order does not matter»

import .Real-mul-comm { mul-comm }

theorem «Real» «multiplication» «parentheses do not matter» (x: Real, y: Real, z: Real) {
    assume «Real» «multiplication» «one is the left identity»(x)
    assume «Real» «multiplication» «one is the right identity»(z)
    assume «Real» «multiplication» «order does not matter»(x, y)
    assume «Real» «multiplication» «order does not matter»(y, z)
    therefore (x * y) * z == x * (y * z)
}

export «Real» «multiplication» «parentheses do not matter»