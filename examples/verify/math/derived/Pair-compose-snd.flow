# @module examples.verify.math.derived
# @means  Second projection commutes with pairing of projections.
# @from   church — https://en.wikipedia.org/wiki/Product_type
# @tier   derived

# @means  snd(pair(fst(p), snd(p))) = snd(p).
# @from   church
# @tier   derived
# @needs  «Pair» «first projection» «first component is retrieved», «Pair» «second projection» «second component is retrieved»

theorem «Pair» «second projection» «second projection factors through pairing» (a: Nat, b: Nat) {
    assume «Pair» «first projection» «first component is retrieved»(a, b)
    assume «Pair» «second projection» «second component is retrieved»(a, b)
    therefore snd(pair(fst(pair(a, b)), snd(pair(a, b)))) == snd(pair(a, b))
}

export «Pair» «second projection» «second projection factors through pairing»