# examples.verify.math.derived

*Proportional magnitudes satisfy alternando.*

**Source.** euclid — Elements, Book V, Proposition 7

## Derived fact 1 — a:b = c:d implies a:c = b:d

**Coordinate.** Ratio · proportion · alternando holds · **Derived fact**

*Source: euclid*

*Built on: same ratio means matching equimultiples, for proportion on Ratio, every magnitude is proportional to itself, for proportion on Ratio*

> **Goal.** a:b = c:d implies a:c = b:d
>
> $$\forall a \in Magnitude \forall b \in Magnitude \forall c \in Magnitude \forall d \in Magnitude\quad ratio(a, c) = ratio(b, d)$$

| | **Proof** | | **Math** |
|:---:|:---|:---:|:---|
| ① | We prove that alternando holds for proportion on Ratio. |  |  |
| ② | We invoke the definitional clause governing proportion on Ratio: same ratio means matching equimultiples, for proportion on Ratio (instantiated for a, b, c, d). |  |  |
| ③ | We invoke the derived fact governing proportion on Ratio: every magnitude is proportional to itself, for proportion on Ratio (instantiated for a). |  |  |
| ④ | From step 2 and step 3, this implies ratio(a, c) equals ratio(b, d). Hence proven. | ④ | $ratio(a, c) = ratio(b, d)$ |

**Trace.** Each step lists the earlier steps it depends on.

| Step | Uses |
|:---:|:---|
| ④ | step 2 and step 3 |

`Ratio · proportion · alternando holds`
