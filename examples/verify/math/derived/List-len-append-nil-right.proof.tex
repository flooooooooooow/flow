\documentclass[11pt]{article}
\usepackage[T1]{fontenc}
\usepackage{textcomp}
\usepackage[margin=0.75in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{array,booktabs}
\usepackage{hyperref}
\setlength{\parindent}{0pt}
\newtheorem{theorem}{Theorem}
\theoremstyle{definition}
\newtheorem{definition}{Definition}
\title{Flow Proof Artifact: List-len-append-nil-right}
\author{Generated by \texttt{flow doc proof}}
\date{Flow Proof Book}
\begin{document}
\maketitle
Appending the empty list on the right preserves length.\\[0.5em]
\textbf{Source.} church — https://en.wikipedia.org/wiki/Length\_of\_a\_list\\[0.5em]
\bigskip
\noindent{\large \textbf{Derived fact 1.} \textit{len(xs ++ nil) = len(xs)} \hfill List \cdot length \cdot append empty on the right preserves length}\par
\smallskip\noindent\textit{Coordinate: List \cdot length \cdot append empty on the right preserves length}\par
\smallskip\noindent\textit{Source: church}\par
\smallskip\noindent\textit{Built on: length adds under append, for length on List, adding zero on the right does not change the number}\par
\medskip
\noindent\textbf{Goal.} len(xs ++ nil) = len(xs)\par
\noindent$\displaystyle \forall xs \in List\quad len(xs ++ nil) = len(xs)$\par
\medskip
\noindent
\begin{tabular}{@{} >{\raggedright\arraybackslash}p{0.06\textwidth} >{\raggedright\arraybackslash}p{0.47\textwidth} >{\raggedleft\arraybackslash}p{0.41\textwidth} @{}}
\textbf{\#} & \textbf{Proof} & \textbf{Mathematics} \\
\midrule
\textbf{1.} & We prove that append empty on the right preserves length for length on List. & \\[0.45em]
\textbf{2.} & We invoke the derived fact governing length on List: length adds under append, for length on List (instantiated for xs, nil). & \\[0.45em]
\textbf{3.} & We invoke the derived fact governing addition on the natural numbers: adding zero on the right does not change the number (instantiated for len(xs)). & $\displaystyle len(xs) + 0 = len(xs)$ \\[0.45em]
\textbf{4.} & From step 2 and step 3, this implies len(xs plus plus nil) equals len(xs). Hence proven. & $\displaystyle len(xs ++ nil) = len(xs)$ \\[0.45em]
\bottomrule
\end{tabular}
\label{thm:List-length-append_empty_on_the_right_preserves_length}
\par\medskip\hrule
\end{document}
