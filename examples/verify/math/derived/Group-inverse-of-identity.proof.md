# examples.verify.math.derived

*The identity element is its own inverse.*

**Source.** dummit-foote — *Abstract Algebra*, §1.1

## Derived fact 1 — inv(1) = 1

**Coordinate.** Group · inverse · the identity is its own inverse · **Derived fact**

*Source: dummit-foote*

*Built on: left inverse recovers the identity, for inverse on Group, one is the right identity, for identity on Group*

> **Goal.** inv(1) = 1
>
> $$inv(1) = 1$$

| | **Proof** | | **Math** |
|:---:|:---|:---:|:---|
| ① | We prove that the identity is its own inverse for inverse on Group. |  |  |
| ② | We invoke the definitional clause governing inverse on Group: left inverse recovers the identity, for inverse on Group (instantiated for 1). |  |  |
| ③ | We invoke the definitional clause governing identity on Group: one is the right identity, for identity on Group (instantiated for 1). |  |  |
| ④ | From step 2 and step 3, this implies inv(1) equals 1. Hence proven. | ④ | $inv(1) = 1$ |

**Trace.** Each step lists the earlier steps it depends on.

| Step | Uses |
|:---:|:---|
| ④ | step 2 and step 3 |

`Group · inverse · the identity is its own inverse`
