function dot(x: memref_f32, y: memref_f32, n: i32) -> f32 {
    let mut acc: f32 = 0.0
    for i in 0..n {
        acc = acc + x[i] * y[i]
    }
    return acc
}

function main() -> i32 {
    return 0
}
