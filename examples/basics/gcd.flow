# Greatest Common Divisor - FLOW version of C++ Euclidean algorithm
function gcd(a: i32, b: i32) -> i32 {
    while b != 0 {
        let temp: i32 = b
        b = a % b
        a = temp
    }
    return a
}

function main() -> i32 {
    let a: i32 = 56
    let b: i32 = 98
    let result: i32 = gcd(a, b)
    # Would print: "GCD of 56 and 98 is 14"
    return result
}
