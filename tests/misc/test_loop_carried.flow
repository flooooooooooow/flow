# Test loop-carried variables (accumulator pattern)

function sum_to_n(n: i32) -> i32 {
    let sum: i32 = 0
    for i in 0..n {
        sum = sum + i
    }
    return sum
}

function main() -> i32 {
    let result: i32 = sum_to_n(10)
    print("Sum 0..10:")
    print(result)
    return 0
}
