# GitHub Actions — disabled

All workflows are renamed `*.yml.disabled` so Actions will not run.

| File | Was |
|------|-----|
| `ci.yml.disabled` | Main CI |
| `wiki.yml.disabled` | Wiki build |
| `docs.yml.disabled` | Docs (already off) |

Re-enable by renaming back to `*.yml`.
