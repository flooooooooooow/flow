name: Setup bare Python
description: Python 3.11 interpreter only — no pip install (Gen0 / flowc bootstrap)

runs:
  using: composite
  steps:
    - uses: actions/setup-python@v6
      with:
        python-version: '3.11'
