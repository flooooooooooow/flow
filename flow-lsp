#!/usr/bin/env python3
"""
FLOW LSP Server Launcher
"""
import sys
import os

# Add this repo's src directory to Python path (relative to this launcher)
sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), 'src'))

# Import and run the LSP server
from flow.lsp_server import main

if __name__ == '__main__':
    main()
